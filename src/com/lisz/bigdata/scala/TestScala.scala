package com.lisz.bigdata.scala

// In Scala, the main method must be put in an object, which is a static singleton
object TestScala {
  def main(args: Array[String]): Unit = {
    println("Hello world!")
  }
}
